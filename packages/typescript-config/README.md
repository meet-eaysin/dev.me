# `@repo/typescript-config`

Shared TypeScript compiler options (`tsconfig.json`) for the Mind Stack monorepo.

## 📦 Usage

This package provides strict typing settings that all apps and packages inherit. It enforces zero-tolerance for `any` and other strict compiler standards.
